export interface WikidataEntity {
  id: string;
  label: string;
  description: string;
}

export interface WikidataSearchResult {
  id: string;
  label: string;
  description: string;
  url: string;
}

export interface Woman {
  id: string;
  name: string;
  description: string;
}
