---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/App.tsx, src/components/InputSection.tsx, src/components/WomenList.tsx, src/components/WomanCard.tsx, src/components/ProgressCounter.tsx]
autonomous: false
requirements: [FR-01.1, FR-01.3, FR-01.4, FR-05.2, FR-05.3]
must_haves:
  truths:
    - "User can type a name and press Enter to search"
    - "If correct, the woman is added to the list and count increases"
    - "If already in the list, the user is notified"
    - "Progress counter (e.g. 0/100) updates correctly"
    - "Loading and error feedback are clearly visible"
  artifacts:
    - path: "src/components/InputSection.tsx"
      provides: "Text input with Enter key support and status feedback"
    - path: "src/components/WomenList.tsx"
      provides: "List rendering for correct entries"
    - path: "src/components/ProgressCounter.tsx"
      provides: "Score display"
  key_links:
    - from: "src/App.tsx"
      to: "src/services/wikidata.ts"
      via: "searchWomen call"
    - from: "src/components/InputSection.tsx"
      to: "src/App.tsx"
      via: "onSearch callback"
---

<objective>
Implement the core game UI components and state management, connecting them to the search service.
Purpose: Create the interactive loop for the game's Phase 1.
Output: Working UI with input, list, counter, and search feedback.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/PHASE_1_RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement UI Components (Shells)</name>
  <files>src/components/InputSection.tsx, src/components/WomenList.tsx, src/components/WomanCard.tsx, src/components/ProgressCounter.tsx</files>
  <action>
    Create basic functional components:
    - `InputSection`: `input` field with `onKeyDown` (Enter) and feedback area (status, error).
    - `ProgressCounter`: Display current count vs goal (100).
    - `WomenList`: Wrapper for the scrollable list of women.
    - `WomanCard`: Card displaying name (label) and optional description.
    Apply basic Vanilla CSS classes for the "Pop" aesthetic.
  </action>
  <verify>Components exist and render basic text.</verify>
  <done>UI component shells are ready for state wiring.</done>
</task>

<task type="auto">
  <name>Task 2: State Management & Search Wiring in App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Implement logic in `App.tsx`:
    - State: `correctWomen` (array of objects), `isLoading` (boolean), `error` (string | null).
    - Function `handleSearch(name)`:
      1. Check if name already in `correctWomen` (case-insensitive label check).
      2. If duplicate, set error.
      3. If new, call `searchWomen(name)`.
      4. If result found, add to `correctWomen` and clear input.
      5. If result null or error, set appropriate error feedback.
    - Wire components: Pass state and callbacks to `InputSection`, `WomenList`, `ProgressCounter`.
  </action>
  <verify>Searching for "Billie Eilish" adds her to the list; "Billie Eilish" again shows error.</verify>
  <done>App state is correctly managed and search logic is integrated.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Animations & Visual Feedback</name>
  <files>src/components/InputSection.tsx, src/components/WomanCard.tsx</files>
  <action>
    Use `framer-motion` for basic feedback:
    - `WomanCard`: Animate entry (fade-in, slide-up).
    - `InputSection`: Shake animation for errors or flash for success.
    Ensure "Pop" aesthetic (high contrast colors, bold borders).
  </action>
  <verify>Cards animate in when added; errors feel visually distinct.</verify>
  <done>Visual feedback is implemented and consistent with the "Pop" aesthetic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 1 interactive flow (Search -> Validate -> Add -> List -> Counter).</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Type "Angela Merkel" and press Enter. Verify she is added.
    3. Type "Angela Merkel" again. Verify "Duplicate" error shows.
    4. Type "Donald Trump". Verify "Not found or not a famous woman" error shows.
    5. Verify counter shows 1/100.
    6. Verify mobile/responsive layout looks reasonable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Manual verification via browser.
Ensure no console errors during the search flow.
</verification>

<success_criteria>
Fully functional basic game loop: input name, verify woman, see progress, see list.
No duplicate names allowed.
Visual feedback for success and failure.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
