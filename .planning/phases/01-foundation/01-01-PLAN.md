---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.ts, src/App.tsx, src/index.css, src/types/wikidata.ts]
autonomous: true
requirements: [FR-01.1, FR-05.3, NFR-02.1]
must_haves:
  truths:
    - "Project scaffolds and runs without errors"
    - "Base layout is present (container, global styles)"
  artifacts:
    - path: "package.json"
      contains: ["react", "typescript", "axios", "lucide-react", "framer-motion"]
    - path: "src/types/wikidata.ts"
      provides: "Wikidata entity and response types"
---

<objective>
Setup the React + TypeScript project using Vite and establish the core folder structure and dependencies.
Purpose: Provide a stable, typed foundation for the search functionality.
Output: Working Vite project with all required libraries and types.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PHASE_1_RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite Project & Install Dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json</files>
  <action>
    Scaffold a new React + TypeScript project using Vite: `npm create vite@latest . -- --template react-ts`.
    Install core dependencies: `axios`, `lucide-react`, `framer-motion`.
    Install dev dependencies: `vitest` (for unit tests in Plan 02).
    Configure `vite.config.ts` if needed (standard config should suffice).
  </action>
  <verify>`npm list axios lucide-react framer-motion vitest` shows all installed.</verify>
  <done>Project is scaffolded and dependencies are installed.</done>
</task>

<task type="auto">
  <name>Task 2: Define Wikidata Types & Folder Structure</name>
  <files>src/types/wikidata.ts, src/services/index.ts, src/components/index.ts</files>
  <action>
    Create the directory structure: `src/services`, `src/components`, `src/types`, `src/styles`.
    Define interfaces in `src/types/wikidata.ts` for:
    - `WikidataEntity`: { id, label, description }
    - `WikidataSearchResponse`: { search: WikidataEntity[] }
    - `WikidataClaimResponse`: For checking P31 and P21 properties.
  </action>
  <verify>Types are defined and directory structure exists.</verify>
  <done>Core types and folder structure are established.</done>
</task>

<task type="auto">
  <name>Task 3: Basic Layout & Global Styles</name>
  <files>src/App.tsx, src/index.css, src/styles/theme.css</files>
  <action>
    Set up global CSS (vibrant/pop theme colors per research).
    Implement a minimal `App.tsx` shell with a main container.
    Use Vanilla CSS for layout (flex/grid).
    Ensure basic accessibility (high contrast text).
  </action>
  <verify>`npm run dev` shows the App shell with applied styles.</verify>
  <done>Basic layout shell is visible and styled.</done>
</task>

</tasks>

<verification>
Run `npm run dev` and ensure the project loads correctly in the browser.
Verify directory structure: services, components, types, styles.
</verification>

<success_criteria>
Project is fully set up with TypeScript, Vite, and all necessary dependencies.
A clean base layout is ready for functional components.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
