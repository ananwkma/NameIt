---
phase: 02-optimistic-ui
plan: 02-02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/App.tsx, src/App.css]
autonomous: true
requirements: [QUEUE-01, QUEUE-02, QUEUE-03]
must_haves:
  truths:
    - "Typing a name and pressing Enter adds it immediately to the list with 30% opacity"
    - "Pending entries are processed one-by-one in the background"
    - "Verified entries illuminate to 100% opacity"
    - "Failed entries are either removed or show error state"
    - "Input remains responsive while verification happens"
  artifacts:
    - path: "src/App.tsx"
      provides: "Background verification queue processor"
    - path: "src/App.css"
      provides: "Styles for pending and verified states"
  key_links:
    - from: "src/App.tsx"
      to: "WikidataService.searchWoman"
      via: "sequential queue processing within a useEffect"
---

<objective>
Implement the core of Phase 2: the Optimistic UI and the background verification queue.

Purpose: Provide instant user feedback and ensure the application remains responsive while processing slow API calls sequentially.
Output: Responsive UI with background queue processing and visual feedback for status.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-optimistic-ui/02-RESEARCH.md
@.planning/phases/02-optimistic-ui/02-01-SUMMARY.md
@src/App.tsx
@src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style Status-based Woman List Items</name>
  <files>src/App.css</files>
  <action>
    Update CSS to handle VerificationStatus:
    1. Define .woman-item.pending style: opacity: 0.3; transition: opacity 0.3s ease-in-out;
    2. Define .woman-item.verified style: opacity: 1; (full illumination).
    3. Define .woman-item.failed style: color: #e53935; text-decoration: line-through; (optional visual cue).
    4. Apply these classes in App.tsx component for list items.
  </action>
  <verify>Manually set a woman state to 'pending' and observe the low opacity.</verify>
  <done>List items reflect their verification status visually.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Background Queue Processor</name>
  <files>src/App.tsx</files>
  <action>
    Implement a sequential background processor in App.tsx:
    1. Update handleSubmit to ONLY dispatch ADD_WOMAN_PENDING (no API call here).
    2. Add a useEffect that watches `state.women` and `state.isProcessing`.
    3. Find the first item in `state.women` with status 'pending'.
    4. If such an item exists and `!isProcessing`, set `isProcessing: true`, call `WikidataService.searchWoman(item.inputName)`.
    5. On success, dispatch `VERIFY_SUCCESS` with Wikidata result.
    6. On failure, dispatch `VERIFY_FAIL`.
    7. Reset `isProcessing: false` after completion (regardless of outcome).
    8. Ensure the input field remains enabled and functional during this process.
  </action>
  <verify>Type multiple names quickly; they should all appear at low opacity and then get verified one by one.</verify>
  <done>Names are verified sequentially in the background while the UI remains responsive.</done>
</task>

</tasks>

<verification>
Perform rapid name entry. Confirm all names appear immediately. Confirm names are processed sequentially (one after another). Confirm successful names transition to 100% opacity.
</verification>

<success_criteria>
- Optimistic UI adds entries immediately at 30% opacity.
- Background processor handles names sequentially.
- Verification success illuminates the entry.
- Input remains responsive throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/02-optimistic-ui/02-02-SUMMARY.md`
</output>
