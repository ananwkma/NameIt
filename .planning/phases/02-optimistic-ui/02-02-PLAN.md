---
phase: 02-optimistic-ui
plan: 02-02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/App.tsx, src/App.css]
autonomous: true
requirements: [QUEUE-01, QUEUE-02, QUEUE-03, UI-GRID-01, UI-ANIM-01]
must_haves:
  truths:
    - "Typing a name and pressing Enter adds it immediately to the list with 30% opacity"
    - "Pending entries are processed one-by-one in the background"
    - "Verified entries illuminate to 100% opacity"
    - "Failed entries dissolve (animate out) and are removed from the list"
    - "The list is displayed as a grid with 5 columns"
    - "Cards are compact/smaller"
  artifacts:
    - path: "src/App.tsx"
      provides: "Background verification queue processor and AnimatePresence list"
    - path: "src/App.css"
      provides: "Grid layout and compact styles"
  key_links:
    - from: "src/App.tsx"
      to: "framer-motion"
      via: "AnimatePresence and motion.div"
---

<objective>
Implement the core of Phase 2: the Optimistic UI and the background verification queue, with a compact grid layout and dissolve animations for failures.

Purpose: Provide instant user feedback, ensure responsiveness, and improve layout density.
Output: Responsive UI with background queue processing, 5-column grid, and dissolve animations.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-optimistic-ui/02-RESEARCH.md
@.planning/phases/02-optimistic-ui/02-01-SUMMARY.md
@src/App.tsx
@src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style Compact Grid & Animations</name>
  <files>src/App.css</files>
  <action>
    Update CSS for layout and sizing:
    1. Update .women-list to use `display: grid; grid-template-columns: repeat(5, 1fr); gap: 0.5rem;`.
    2. Style .woman-card to be compact (smaller padding, font-size).
    3. Ensure .woman-card.pending has 0.3 opacity.
    4. Remove .woman-card.failed styles (handled via animation/removal).
  </action>
  <verify>Check that items appear in a 5-column grid and look compact.</verify>
  <done>Grid layout and compact styles defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Queue & AnimatePresence</name>
  <files>src/App.tsx</files>
  <action>
    Implement the queue and animation logic in App.tsx:
    1. Import `motion` and `AnimatePresence` from `framer-motion`.
    2. Wrap the list rendering in `<AnimatePresence>` and use `<motion.div>` for cards.
    3. Configure `exit={{ opacity: 0, scale: 0.9 }}` for the dissolve effect.
    4. Implement the `useEffect` queue processor:
       - Watch `state.women` and `state.isProcessing`.
       - Find first 'pending' item.
       - Call `WikidataService.searchWoman`.
       - On SUCCESS: Dispatch `VERIFY_SUCCESS`.
       - On FAIL: Dispatch `REMOVE_WOMAN` (Need to add this action to reducer or reuse VERIFY_FAIL with a side effect to remove). *Better approach: Dispatch VERIFY_FAIL to mark it, then have a useEffect or the Reducer remove it after a delay?* 
       - *Revised approach:* Dispatch `VERIFY_FAIL` which removes it from the state immediately. `AnimatePresence` handles the dissolve.
    5. Update `gameReducer` (inside App.tsx or imported) to handle `VERIFY_FAIL` by removing the item from the array instead of marking it 'failed'.
  </action>
  <verify>Add valid name -> 100% opacity. Add invalid name -> Dissolves away. Grid layout is maintained.</verify>
  <done>Queue processor works, invalid items dissolve, layout is 5-column grid.</done>
</task>

</tasks>

<verification>
Start game. Type valid name -> Appears low opacity -> Becomes full opacity. Type invalid name -> Appears low opacity -> Dissolves/Disappears. Verify 5 items per row.
</verification>

<success_criteria>
- 5-column grid layout.
- Compact card design.
- Failed items animate out (dissolve).
- Background queue processes items sequentially.
</success_criteria>

<output>
After completion, create `.planning/phases/02-optimistic-ui/02-02-SUMMARY.md`
</output>
