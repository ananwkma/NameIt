---
phase: 02-optimistic-ui
plan: 02-03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [src/App.tsx, src/types/game.ts]
autonomous: true
requirements: [TIMER-01, TIMER-02, STATE-01]
must_haves:
  truths:
    - "User can choose between CLASSIC and SPEEDRUN modes"
    - "CLASSIC mode counts down from 15:00 and shows Game Over on expiry"
    - "SPEEDRUN mode stopwatch records time after 100 entries"
    - "Refreshing the page restores the current game state"
  artifacts:
    - path: "src/App.tsx"
      provides: "Timer logic and persistence handling"
  key_links:
    - from: "src/App.tsx"
      to: "localStorage"
      via: "useEffect to persist state"
---

<objective>
Implement game modes (Classic and Speedrun) and state persistence to local storage.

Purpose: Add game-like mechanics and ensure users don't lose progress on refresh.
Output: Working timer system with two game modes and persistent game state.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-optimistic-ui/02-RESEARCH.md
@.planning/phases/02-optimistic-ui/02-01-SUMMARY.md
@.planning/phases/02-optimistic-ui/02-02-SUMMARY.md
@src/App.tsx
@src/types/game.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Timer Logic</name>
  <files>src/App.tsx, src/types/game.ts</files>
  <action>
    Implement timer system in App.tsx:
    1. Update GameState with:
       - timeLeft (number, ms)
       - startTime (number, ms - timestamp)
       - timeElapsed (number, ms)
    2. Add START_GAME action to initialize game state for chosen mode.
    3. Implement a timer useEffect:
       - Only run when state.status === 'PLAYING'.
       - Use setInterval (100ms) to update state.timeLeft (decrement) or state.timeElapsed (increment) based on performance.now().
       - Trigger GAME_OVER when timeLeft <= 0 in CLASSIC mode.
       - Trigger win condition when women.length >= 100 in SPEEDRUN mode.
    4. Render the timer in the UI.
  </action>
  <verify>Start a Classic game and confirm the countdown; start a Speedrun game and confirm the stopwatch.</verify>
  <done>Game modes have working timers and game-over conditions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement State Persistence</name>
  <files>src/App.tsx</files>
  <action>
    Add localStorage persistence:
    1. Add a LOAD_GAME action to restore state.
    2. Use a useEffect to initialize the state from 'game_state' in localStorage on mount.
    3. Use a useEffect to save the current state to localStorage whenever it changes (consider debounce or only on specific state updates).
    4. Implement high score persistence: when a game ends, save the score (count or time) to a separate 'high_scores' key.
  </action>
  <verify>Play a game, add some names, refresh the browser; confirm the names and timer state are restored.</verify>
  <done>Game progress is preserved across page refreshes.</done>
</task>

</tasks>

<verification>
Start a game, add names, wait for verification. Confirm timer is running. Refresh page. Confirm all state is restored (including pending/verified items and current time). Confirm game ends correctly on time expiry.
</verification>

<success_criteria>
- Timer works correctly for both game modes.
- Game ends on time expiry in Classic mode.
- State is persistent in local storage.
- High scores are saved.
</success_criteria>

<output>
After completion, create `.planning/phases/02-optimistic-ui/02-03-SUMMARY.md`
</output>
