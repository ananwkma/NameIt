---
phase: 02-optimistic-ui
plan: 02-03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [src/App.tsx, src/types/game.ts, src/App.css]
autonomous: true
requirements: [TIMER-01, TIMER-02, STATE-01]
must_haves:
  truths:
    - "Game starts immediately with a 15-minute countdown (Standard Mode)"
    - "At 00:00, game pauses and shows a 'Time's Up' modal with current score"
    - "User can choose 'Continue' to enter Zen Mode (timer counts up from 15:00)"
    - "User can choose 'Stop' to end the game and see results"
    - "Reaching 100 verified entries triggers Victory screen with total time"
    - "Refreshing the page restores the current game state"
  artifacts:
    - path: "src/App.tsx"
      provides: "Timer logic, Zen Mode transition, and persistence handling"
    - path: "src/types/game.ts"
      provides: "Updated GameState with zenMode flag"
  key_links:
    - from: "src/App.tsx"
      to: "localStorage"
      via: "useEffect to persist state"
---

<objective>
Implement the default 15-minute timer, Zen Mode transition, and state persistence.

Purpose: Provide a seamless game loop where players can choose to extend their session beyond the time limit.
Output: Working timer system with Standard/Zen modes and persistent game state.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-optimistic-ui/02-RESEARCH.md
@.planning/phases/02-optimistic-ui/02-01-SUMMARY.md
@.planning/phases/02-optimistic-ui/02-02-SUMMARY.md
@src/App.tsx
@src/types/game.ts
@src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Timer & Zen Mode Logic</name>
  <files>src/App.tsx, src/types/game.ts, src/App.css</files>
  <action>
    Implement timer system in App.tsx:
    1. Update GameState in `src/types/game.ts`:
       - Remove `mode` (implicitly starts as Standard).
       - Add `isZenMode: boolean`.
       - Add `timeLeft` (for countdown) and `timeElapsed` (for total time tracking).
    2. In `App.tsx`, implement the timer `useEffect`:
       - If `!isZenMode`: Decrement `timeLeft`. If `timeLeft <= 0`, pause game (`status: 'TIME_UP'`).
       - If `isZenMode`: Increment `timeElapsed` (starting from 15 mins).
    3. Implement `TIME_UP` Modal:
       - Show score (X/100).
       - "Continue" button -> Dispatch `ENTER_ZEN_MODE` (sets `isZenMode: true`, `status: 'PLAYING'`, `timeElapsed` starts at 15m).
       - "Stop" button -> Dispatch `END_GAME`.
    4. Update Victory condition: Trigger when verified count >= 100.
    5. Style the modal and timer display.
  </action>
  <verify>Start game, let timer run out (or mock time). Verify modal appears. Click Continue, verify timer counts up. Reach 100, verify victory.</verify>
  <done>Timer logic handles 15m countdown -> Modal -> Zen Mode transition.</done>
</task>

<task type="auto">
  <name>Task 2: Implement State Persistence</name>
  <files>src/App.tsx</files>
  <action>
    Add localStorage persistence:
    1. Add `LOAD_GAME` action to restore state.
    2. Use `useEffect` to load state from `game_state` on mount.
    3. Use `useEffect` to save state to `game_state` on every change (debounced/throttled if needed).
    4. Ensure `isZenMode` and timer values are persisted.
  </action>
  <verify>Play game, enter Zen Mode, refresh page. Confirm game resumes in Zen Mode with correct time.</verify>
  <done>Game progress is preserved across page refreshes.</done>
</task>

</tasks>

<verification>
Start a game. Refresh page -> Timer resumes. Wait for time up -> Modal appears. "Continue" -> Zen Mode starts. Refresh page -> Zen Mode resumes. Complete 100 -> Victory screen.
</verification>

<success_criteria>
- Game starts with 15m countdown.
- Time Up modal appears at 00:00.
- Zen Mode continues correctly.
- Persistence works for all states.
</success_criteria>

<output>
After completion, create `.planning/phases/02-optimistic-ui/02-03-SUMMARY.md`
</output>
