---
phase: 02-optimistic-ui
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/game.ts, src/App.tsx]
autonomous: true
requirements: [QUEUE-01, QUEUE-02]
must_haves:
  truths:
    - "Application uses useReducer for centralized state management"
    - "GameWoman type supports pending, verified, and failed status"
    - "GameState includes game mode and status tracking"
  artifacts:
    - path: "src/types/game.ts"
      provides: "Updated GameState and GameAction types"
    - path: "src/App.tsx"
      provides: "Refactored state management using useReducer"
  key_links:
    - from: "src/App.tsx"
      to: "useReducer"
      via: "hook call for state management"
---

<objective>
Establish the architectural foundation for Phase 2 by migrating App state to a useReducer and updating game types to support optimistic UI and status tracking.

Purpose: Provide a robust, centralized state structure capable of handling asynchronous background verification and multiple game modes.
Output: Refactored App.tsx with useReducer and enhanced game.ts types.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-optimistic-ui/02-RESEARCH.md
@src/types/game.ts
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Game Types</name>
  <files>src/types/game.ts</files>
  <action>
    Update src/types/game.ts to:
    1. Define VerificationStatus = 'pending' | 'verified' | 'failed'.
    2. Define GameWoman extending Woman (from wikidata) with: status (VerificationStatus), tempId (string), and inputName (string).
    3. Update GameState to include:
       - status: 'IDLE' | 'PLAYING' | 'PAUSED' | 'GAME_OVER'
       - mode: 'CLASSIC' | 'SPEEDRUN'
       - women: GameWoman[] (replacing Woman[])
       - isProcessing: boolean
       - error: string | null
    4. Define GameAction union for:
       - START_GAME (payload: GameMode)
       - ADD_WOMAN_PENDING (payload: { name: string, tempId: string })
       - VERIFY_SUCCESS (payload: { tempId: string, data: Woman })
       - VERIFY_FAIL (payload: { tempId: string })
       - SET_PROCESSING (payload: boolean)
       - SET_ERROR (payload: string | null)
  </action>
  <verify>Run 'npm run tsc' or ensure no type errors in IDE.</verify>
  <done>GameWoman and GameState types support status-based list items and queue processing state.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor App to useReducer</name>
  <files>src/App.tsx</files>
  <action>
    Refactor state management in src/App.tsx:
    1. Implement a gameReducer that handles the actions defined in Task 1.
    2. Replace existing useState calls (women, error, etc.) with a single useReducer(gameReducer, initialState).
    3. Update handleSubmit to dispatch ADD_WOMAN_PENDING immediately, and temporarily handle the verification logic within the same function by dispatching VERIFY_SUCCESS/FAIL after the API call (this will be moved to the queue processor in the next plan).
    4. Update the render logic to handle the new GameWoman structure (use tempId for keys).
  </action>
  <verify>App still runs and allows adding women; UI reflects the current state accurately.</verify>
  <done>App.tsx uses useReducer for all game-related state with no functional regressions.</done>
</task>

</tasks>

<verification>
Ensure the application starts, allows starting a game, and adding names results in them appearing in the list using the new state structure.
</verification>

<success_criteria>
- GameWoman type includes status and tempId.
- App.tsx state managed via useReducer.
- Names can still be added and verified (synchronously for now).
</success_criteria>

<output>
After completion, create `.planning/phases/02-optimistic-ui/02-01-SUMMARY.md`
</output>
