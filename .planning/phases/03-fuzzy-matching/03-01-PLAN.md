---
phase: 03-fuzzy-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
- src/utils/fuzzyMatch.ts
- src/utils/fuzzyMatch.test.ts
- src/services/wikidata.ts
- src/services/wikidata.test.ts
autonomous: true
requirements:
- PHASE3-FM-1
- PHASE3-RANK-2
- PHASE3-PERF-3
- PHASE3-ALIAS-5
user_setup: []
must_haves:
  truths:
  - Fuzzy matching correctly identifies names within the specified tolerance ('2-char diff OR 80% similarity').
  - WikidataService fetches `sitelinks` for fame ranking when retrieving entity details.
  - WikidataService uses cached results for repeated searches to reduce API calls.
  - WikidataService retrieves property labels from cache when available.
  - WikidataService performs alias checking using fuzzy matching alongside primary label matching.
  - WikidataService fetches details only for the top 5 candidates from search results.
  artifacts:
  - path: "src/utils/fuzzyMatch.ts"
    provides: "Fuzzy matching utility functions (Levenshtein, similarity ratio, combined match)."
  - path: "src/utils/fuzzyMatch.test.ts"
    provides: "Unit tests for fuzzy matching logic."
  - path: "src/services/wikidata.ts"
    provides: "Refactored Wikidata service with enhanced search, ranking, caching, and alias support."
    contains: "function fuzzyMatchNames"
    contains: "sitelinks"
    contains: "cache"
    contains: "top 5"
  - path: "src/services/wikidata.test.ts"
    provides: "Unit tests for refactored WikidataService."
  key_links:
  - from: "src/services/wikidata.ts"
    to: "src/utils/fuzzyMatch.ts"
    via: "Calling fuzzyMatchNames for alias checking."
    pattern: "fuzzyMatchNames\("
  - from: "src/services/wikidata.ts"
    to: "Wikidata API (mocked)"
    via: "Fetching entity details including sitelinks."
    pattern: "fetch\(.*entity\)"
  - from: "src/services/wikidata.ts"
    to: "In-memory cache"
    via: "Checking and setting search results and property labels."
    pattern: "cache\.get\||cache\.set\|Map\."
---

<objective>
Implement core fuzzy matching logic, enhance the Wikidata service for ranking and performance, and establish caching mechanisms.
Purpose: To lay the foundational logic for accurate name matching, improve search efficiency, and enable fame ranking based on Wikidata data.
Output:
- `src/utils/fuzzyMatch.ts` with implemented fuzzy matching algorithms.
- Refactored `src/services/wikidata.ts` with caching, alias handling, and ranking capabilities.
- Comprehensive unit tests for both modules.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Relevant previous plan summaries are loaded here if needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Fuzzy Matching Utilities</name>
  <files>src/utils/fuzzyMatch.ts, src/utils/fuzzyMatch.test.ts</files>
  <action>
Create `src/utils/fuzzyMatch.ts`. Implement functions for Levenshtein distance calculation, similarity ratio computation, and a combined `fuzzyMatchNames` function. The `fuzzyMatchNames` function must adhere to the rule: "2-character difference OR 80% similarity".
Add comprehensive unit tests in `src/utils/fuzzyMatch.test.ts` covering edge cases, different string lengths, and various similarity/difference scenarios to ensure accuracy.
  </action>
  <verify>Run unit tests specifically for fuzzy matching utilities: `npm test -- src/utils/fuzzyMatch.test.ts`</verify>
  <done>The `src/utils/fuzzyMatch.ts` file contains correct implementations for Levenshtein distance, similarity ratio, and the combined fuzzy matching logic. All unit tests in `src/utils/fuzzyMatch.test.ts` pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor WikidataService for Ranking, Caching, and Alias Matching</name>
  <files>src/services/wikidata.ts, src/services/wikidata.test.ts</files>
  <action>
Modify `src/services/wikidata.ts` to:
- Fetch `sitelinks` when retrieving entity details, which will be used for fame ranking.
- Integrate the `fuzzyMatchNames` utility from `src/utils/fuzzyMatch.ts` to perform alias checking against both primary labels and aliases found in Wikidata results.
- Implement an in-memory cache (e.g., using `Map`) for Wikidata search results to avoid redundant API calls for identical queries.
- Implement an in-memory cache for Wikidata property labels to speed up label retrieval.
- Add logic to fetch full entity details only for the top 5 candidate results from an initial search, optimizing performance.
Update `src/services/wikidata.test.ts` to comprehensively test these new functionalities. This includes mocking API calls, verifying cache hits and misses, confirming correct ranking logic based on `sitelinks`, testing alias matching accuracy, and ensuring the top 5 candidate filtering works as expected.
  </action>
  <verify>Run unit tests specifically for the refactored Wikidata service: `npm test -- src/services/wikidata.test.ts`</verify>
  <done>The `src/services/wikidata.ts` has been refactored to correctly fetch `sitelinks`, perform alias matching using fuzzy logic, utilize in-memory caches for search results and property labels, and filter candidates to the top 5 for detail fetching. All tests in `src/services/wikidata.test.ts` pass.</done>
</task>

</tasks>

<verification>
All implemented logic for fuzzy matching and Wikidata service enhancements are verified through unit tests.
</verification>

<success_criteria>
The fuzzy matching utilities are correctly implemented and tested. The Wikidata service is refactored to support fame ranking via sitelinks, improved alias checking with fuzzy matching, efficient caching, and performance optimizations by fetching details only for top candidates. All associated unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fuzzy-matching/03-01-SUMMARY.md`
</output>
