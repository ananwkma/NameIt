---
phase: "03-fuzzy-matching"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["package.json", "src/utils/fuzzyMatch.ts", "src/utils/fuzzyMatch.test.ts", "src/types/wikidata.ts", "src/services/wikidata.ts", "src/services/wikidata.test.ts", "src/types/game.ts", "src/state/gameReducer.ts", "src/state/gameReducer.test.ts"]
autonomous: true
requirements: ["FR-04.1", "FR-03.3", "FR-03.4", "NFR-01.1", "NFR-01.2", "FR-01.4"]
user_setup: []

must_haves:
  truths:
    - "Inputs with 1-2 typos correctly match famous women (e.g., 'Billie Elish' -> Billie Eilish)"
    - "Common nicknames/aliases match (e.g., 'IU' -> Lee Ji-eun)"
    - "Search results prioritize well-known figures (high sitelinks) over obscure ones"
    - "Game state can track 'pending' names that are being verified"
    - "Reducer handles moving names from pending to confirmed or removing them on failure"
  artifacts:
    - path: "src/utils/fuzzyMatch.ts"
      provides: "Threshold logic (2-char diff or 80% similarity)"
    - path: "src/services/wikidata.ts"
      provides: "Fame-ranked search with early filtering and caching"
    - path: "src/state/gameReducer.ts"
      provides: "Queue-aware state transitions (ADD_PENDING, RESOLVE_PENDING)"
  key_links:
    - from: "src/services/wikidata.ts"
      to: "src/utils/fuzzyMatch.ts"
      via: "isMatch"
    - from: "src/state/gameReducer.ts"
      to: "src/types/game.ts"
      via: "pendingWomen array"
---

<objective>
Implement core logic for fuzzy matching, fame ranking, and alias handling, and update the game state to support a verification queue.

Purpose: Increase input tolerance, improve speed/reliability, and enable optimistic UI updates.
Output: Fuzzy matching utility, optimized service layer, and queue-ready game state.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/wikidata.ts
@src/services/wikidata.ts
@src/types/game.ts
@src/state/gameReducer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fuzzyMatch utility and Refactor WikidataService</name>
  <files>package.json, src/utils/fuzzyMatch.ts, src/utils/fuzzyMatch.test.ts, src/types/wikidata.ts, src/services/wikidata.ts, src/services/wikidata.test.ts</files>
  <action>
    1. Install `fuzzball` via `npm install fuzzball`.
    2. Create `src/utils/fuzzyMatch.ts`:
       - `isMatch(input, target)`: Returns `true` if:
         - Levenshtein distance <= 2 (using `fuzzball.distance`)
         - OR `fuzzball.ratio(input, target)` >= 80.
       - Case-insensitive, trimmed.
    3. Update `Woman` in `src/types/wikidata.ts`: Add `aliases: string[]`, `sitelinks: number`.
    4. Refactor `searchWoman` in `src/services/wikidata.ts`:
       - Use `isMatch` to perform **Early Filtering** on `wbsearchentities` results.
       - Select only **top 5** candidates based on label/description match.
       - Fetch `aliases` and `sitelinks` in `wbgetentities`.
       - **Fame Ranking**: Sort final candidates by `sitelinks` (descending).
       - Iterate through primary `name` and all `aliases` for final verification.
       - Implement **In-Memory Caching** for searches (Input -> Result) and labels (QID -> Label).
    5. Verify with tests for typos, aliases, and ranking.
  </action>
  <verify>`npx vitest src/utils/fuzzyMatch.test.ts src/services/wikidata.test.ts` passes.</verify>
  <done>Service layer is optimized and supports fuzzy/fame-ranked search.</done>
</task>

<task type="auto">
  <name>Task 2: Update Game State for Verification Queue support</name>
  <files>src/types/game.ts, src/state/gameReducer.ts, src/state/gameReducer.test.ts</files>
  <action>
    1. Update `GameState` in `src/types/game.ts`:
       - Add `pendingWomen: string[]`.
    2. Update `GameAction` in `src/types/game.ts`:
       - Add `ADD_PENDING`: payload is `string` (the name typed by user).
       - Add `RESOLVE_PENDING_SUCCESS`: payload `{ pendingName: string, woman: Woman }`.
       - Add `RESOLVE_PENDING_FAILURE`: payload `{ pendingName: string, error?: string }`.
    3. Update `gameReducer.ts`:
       - `ADD_PENDING`: Add name to `pendingWomen` (prevent duplicates if already in `women` or `pendingWomen`).
       - `RESOLVE_PENDING_SUCCESS`: Add `woman` to `women`, remove `pendingName` from `pendingWomen`, handle victory check.
       - `RESOLVE_PENDING_FAILURE`: Remove `pendingName` from `pendingWomen`.
    4. Update `gameReducer.test.ts` to cover these new actions.
  </action>
  <verify>`npx vitest src/state/gameReducer.test.ts` passes.</verify>
  <done>Game state supports tracking and resolving pending verifications.</done>
</task>

</tasks>

<verification>
- Verify `isMatch` follows the 2-char/80% rule.
- Verify `searchWoman` prioritizes famous women and caches results.
- Verify `gameReducer` correctly transitions from `pending` to `women`.
</verification>

<success_criteria>
- Optimized search with typo/alias support.
- Game state correctly manages optimistic 'pending' entries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fuzzy-matching/03-01-SUMMARY.md`
</output>
