---
phase: "03-fuzzy-matching"
plan: "02"
type: "execute"
wave: 2
depends_on: ["03-01"]
files_modified: ["src/hooks/useVerificationQueue.ts", "src/components/Screens/GameLoop.tsx", "src/components/Game/InputArea.tsx", "src/components/Game/WomanList.tsx"]
autonomous: false
requirements: ["FR-05.2", "NFR-01.1", "FR-01.3"]
user_setup: []

must_haves:
  truths:
    - "User sees name in the list immediately after pressing Enter, with 'pending' style"
    - "Names in 'pending' state are automatically verified by the background loop"
    - "If verification fails, the name is removed and a notification is shown"
    - "If verification succeeds, the name transitions to full opacity/confirmed style"
  artifacts:
    - path: "src/hooks/useVerificationQueue.ts"
      provides: "Queue processing loop that calls WikidataService"
    - path: "src/components/Game/WomanList.tsx"
      provides: "Unified list rendering of confirmed and pending names"
  key_links:
    - from: "src/hooks/useVerificationQueue.ts"
      to: "src/services/wikidata.ts"
      via: "searchWoman call"
    - from: "src/components/Screens/GameLoop.tsx"
      to: "src/hooks/useVerificationQueue.ts"
      via: "hook usage"
---

<objective>
Implement the verification queue processing loop and update the UI for optimistic rendering and background verification feedback.

Purpose: Provide immediate feedback to the user and handle API latency in the background.
Output: Verification queue hook and updated UI components.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fuzzy-matching/03-01-SUMMARY.md
@src/services/wikidata.ts
@src/components/Screens/GameLoop.tsx
@src/components/Game/InputArea.tsx
@src/components/Game/WomanList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement useVerificationQueue hook</name>
  <files>src/hooks/useVerificationQueue.ts</files>
  <action>
    1. Create `src/hooks/useVerificationQueue.ts`:
       - This hook should take `state` and `dispatch` from the game state.
       - Use `useEffect` to watch `state.pendingWomen`.
       - If `pendingWomen` is not empty, process the first item:
         - Call `WikidataService.searchWoman(pendingName)`.
         - On success: Dispatch `RESOLVE_PENDING_SUCCESS`.
         - On failure: Dispatch `RESOLVE_PENDING_FAILURE` with error message.
         - Ensure only one verification runs at a time (use a `processing` ref or state).
       - Add a `notifications` state to track transient errors (e.g., "Could not find 'X'") and provide a way to clear them.
  </action>
  <verify>The hook correctly processes names from the queue and dispatches the appropriate actions.</verify>
  <done>Verification queue logic is implemented as a reusable hook.</done>
</task>

<task type="auto">
  <name>Task 2: Update GameLoop and InputArea for Queue Submission</name>
  <files>src/components/Screens/GameLoop.tsx, src/components/Game/InputArea.tsx</files>
  <action>
    1. In `GameLoop.tsx`:
       - Integrate `useVerificationQueue`.
       - Update `handleAdd` to dispatch `ADD_PENDING` immediately.
       - Render the transient notifications from the hook (e.g., as toasts or status text).
    2. In `InputArea.tsx`:
       - Ensure `isLoading` is only true if the queue is processing.
       - Disable input only if a certain threshold of pending items is reached (optional, otherwise keep it enabled for speed).
  </action>
  <verify>Submitting a name adds it to the list instantly; loading state reflects background work.</verify>
  <done>UI components are wired to the verification queue.</done>
</task>

<task type="auto">
  <name>Task 3: Update WomanList for Pending State Rendering</name>
  <files>src/components/Game/WomanList.tsx</files>
  <action>
    1. Update `WomanList.tsx` to accept both `women` and `pendingWomen` (or a unified list).
    2. Render confirmed women as before.
    3. Render pending women with:
       - Lower opacity (e.g., `opacity: 0.6`).
       - A "Verifying..." label or subtle pulse animation.
    4. Ensure the list is sorted correctly (e.g., newest at the top).
  </action>
  <verify>User sees their typed name in the list with a 'pending' style immediately after submission.</verify>
  <done>List rendering reflects both confirmed and pending verification states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final Verification of Verification Queue UX</name>
  <what-built>Verification queue with optimistic rendering and background processing.</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Type "Billie Elish" and press Enter.
    3. Observe "Billie Elish" appears in the list immediately with reduced opacity.
    4. Observe it changes to "Billie Eilish" (confirmed) after a moment.
    5. Type a nonsensical name: observe it appears pending, then disappears, and a transient error message is shown.
    6. Rapidly type 3-4 names: observe them queueing up and resolving one by one.
  </how-to-verify>
  <resume-signal>Approved</resume-signal>
</task>

</tasks>

<verification>
- Optimistic rendering works (no delay between Enter and UI update).
- Background queue processes verifications sequentially.
- Failures result in removal and error feedback.
- Successes update the list with full data.
</verification>

<success_criteria>
- Verification latency is hidden from the user.
- UI remains responsive during background API calls.
- Clear distinction between pending and confirmed entries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fuzzy-matching/03-02-SUMMARY.md`
</output>
