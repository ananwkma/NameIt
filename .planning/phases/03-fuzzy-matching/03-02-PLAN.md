---
phase: 03-fuzzy-matching
plan: 02
type: execute
wave: 2
depends_on:
- "03-fuzzy-matching-01"
files_modified:
- src/components/InputArea.tsx
- src/types/game.ts
- src/state/gameReducer.ts
- src/components/GameLoop.tsx
- src/components/WomanList.tsx
- src/components/InputArea.test.ts
- src/state/gameReducer.test.ts
- src/components/WomanList.test.ts
autonomous: true
requirements:
- PHASE3-QUEUE-4
- PHASE3-UI-6
- PHASE3-PERF-DEBOUNCE-3
user_setup: []
must_haves:
  truths:
  - User input is debounced (300-500ms) before search is triggered.
  - User sees dynamic status messages like 'Searching...', 'Verifying...', and specific error messages.
  - Submitted names appear in a 'pending' state with low visual opacity.
  - Successfully verified names move to the main 'women' list with full opacity and increment the score counter.
  - Failed or duplicate submissions are removed from the pending queue and display an appropriate error message.
  - The verification queue processes submitted names asynchronously.
  artifacts:
  - path: "src/components/InputArea.tsx"
    provides: "User input component with debouncing, status feedback, and queue submission."
    contains: "debounce"
    contains: "status"
    contains: "pendingWomen"
  - path: "src/types/game.ts"
    provides: "Updated GameState interface including 'pendingWomen: string[]'."
    contains: "pendingWomen"
  - path: "src/state/gameReducer.ts"
    provides: "Redux reducer logic for managing the 'pendingWomen' queue and related state updates."
    contains: "pendingWomen"
  - path: "src/components/GameLoop.tsx"
    provides: "Component managing asynchronous processing of the verification queue."
    contains: "processQueue"
    contains: "dispatch"
  - path: "src/components/WomanList.tsx"
    provides: "Component to display confirmed women with correct visual state."
    contains: "women"
    contains: "opacity"
  - path: "src/components/InputArea.test.ts"
    provides: "Component tests for InputArea focusing on debouncing, status messages, and queue interaction."
  - path: "src/state/gameReducer.test.ts"
    provides: "Reducer tests for pendingWomen state management."
  - path: "src/components/WomanList.test.ts"
    provides: "Component tests for WomanList displaying pending/confirmed states."
  key_links:
  - from: "src/components/InputArea.tsx"
    to: "src/services/wikidata.ts"
    via: "Debounced search calls, triggering WikidataService."
    pattern: "debouncedSearch\(.*WikidataService\|fetchEntityDetails\)"
  - from: "src/components/InputArea.tsx"
    to: "src/state/gameReducer.ts"
    via: "Dispatching actions to add to pendingWomen queue."
    pattern: "dispatch\(addPendingWoman\|ADD_PENDING_WOMAN\)"
  - from: "src/components/GameLoop.tsx"
    to: "src/state/gameReducer.ts"
    via: "Dispatching actions to process queue results (success/failure)."
    pattern: "dispatch\(moveWomanToConfirmed\|REMOVE_PENDING_WOMAN\)"
  - from: "src/components/GameLoop.tsx"
    to: "src/services/wikidata.ts (indirectly)"
    via: "Initiating verification process for pending women."
    pattern: "processQueue.*WikidataService"
  - from: "src/components/WomanList.tsx"
    to: "GameState"
    via: "Reading confirmed women and pending women."
    pattern: "state\.women\|state\.pendingWomen"
---

<objective>
Integrate the fuzzy matching and ranking logic into the user interface, implement the asynchronous verification queue system, and provide clear user feedback for a seamless gameplay experience.
Purpose: To connect the backend logic with the frontend, enabling users to submit names, have them verified efficiently, and see their status and results dynamically.
Output:
- Updated `InputArea.tsx` with debouncing, status messages, and queue submission.
- Modified `GameState` and `gameReducer` for queue management.
- `GameLoop.tsx` to handle asynchronous queue processing.
- Component tests for UI integration and state management.
</objective>

<execution_context>
@C:/Users/ananW/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/ananW/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Plan 03-fuzzy-matching-01 summary to be referenced for WikidataService and fuzzyMatch utilities.
@.planning/phases/03-fuzzy-matching/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Input Debouncing, UI Feedback, and State Updates</name>
  <files>src/components/InputArea.tsx, src/types/game.ts, src/state/gameReducer.ts</files>
  <action>
Update `src/components/InputArea.tsx` to implement input debouncing for search queries, using a delay of 300-500ms. The debounced search function should call the refactored `WikidataService` (from Plan 01).
Integrate UI feedback states within `InputArea.tsx` to display messages such as "Searching...", "Verifying...", and specific error messages (e.g., "Duplicate name found", "Name not found by Wikidata").
Modify `src/types/game.ts` to add a new property `pendingWomen: string[]` to the `GameState` interface.
Update `src/state/gameReducer.ts` to include reducer logic for managing the `pendingWomen` array: adding new entries, removing them upon successful verification, and removing them upon failure or duplicate detection. Ensure this logic correctly handles counter increments for confirmed women.
Add component tests in `src/components/InputArea.test.ts` to verify debouncing, status message rendering, and correct dispatching of actions for queue submission. Add tests in `src/state/gameReducer.test.ts` for the new `pendingWomen` state management logic.
  </action>
  <verify>Run tests: `npm test -- src/components/InputArea.test.ts src/state/gameReducer.test.ts`.</verify>
  <done>Input fields in `InputArea.tsx` are debounced. Dynamic status and error messages are displayed correctly. The `GameState` in `src/types/game.ts` includes `pendingWomen`, and `src/state/gameReducer.ts` correctly manages additions and removals from this array and updates other relevant state like counters. All specified tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Asynchronous Verification Queue and UI Integration</name>
  <files>src/components/GameLoop.tsx, src/components/InputArea.tsx, src/components/WomanList.tsx, src/components/InputArea.test.ts, src/components/WomanList.test.ts</files>
  <action>
Modify `src/components/GameLoop.tsx` to implement the asynchronous processing of the `pendingWomen` queue. This involves creating a function that iterates through the `pendingWomen` array. For each name, it should trigger the verification process (likely calling a function that utilizes the refactored `WikidataService` from Plan 01). Based on the verification outcome (success, failure, duplicate), dispatch appropriate actions to the `gameReducer` to move the name to the `women` list, remove it from `pendingWomen`, update the score counter, or display an error.
Update `src/components/InputArea.tsx` to correctly add submitted names to the `pendingWomen` queue in `GameState` and visually represent these pending names with low opacity.
Ensure `src/components/WomanList.tsx` (or the component responsible for displaying confirmed women) correctly renders the `women` list with full opacity and reflects any score counter updates.
Add component tests in `src/components/InputArea.test.ts` and `src/components/WomanList.test.ts` to verify the interaction with the queue, visual states (pending, confirmed), and overall flow.
  </action>
  <verify>Run tests: `npm test -- src/components/InputArea.test.ts src/components/WomanList.test.ts`.</verify>
  <done>The verification queue in `GameLoop.tsx` processes pending names asynchronously. Submitted names appear in `InputArea.tsx` with low opacity, moving to `WomanList.tsx` with full opacity upon successful verification and incrementing the score. Errors and duplicates are handled gracefully by removing the name from pending and displaying messages. Component tests pass.</done>
</task>

</tasks>

<verification>
All UI integration, state management, and queue processing logic are verified through component tests and by observing the application's behavior during manual testing.
</verification>

<success_criteria>
The user interface effectively handles name submission with debouncing and visual feedback. The asynchronous verification queue correctly processes submitted names, updating the game state and UI accordingly. Successfully verified names are added to the main list with full opacity and increment the score, while failures are handled with error messages. All related component and reducer tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fuzzy-matching/03-02-SUMMARY.md`
</output>
